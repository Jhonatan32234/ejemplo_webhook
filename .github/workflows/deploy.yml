name: Workflow Example

on:
  push:
    branches:
      - develop

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Decir hola
              run: echo "Hola, mundo!"

            - name: Decir adios
              run: echo ADIOS
